// import analogous from "./analogous";

describe("analogous function", () => {
  it("should return an array of colors that are analogous to the given color", () => {
      expect(2).toBe(2);
  });
});
